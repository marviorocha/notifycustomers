<%= render 'shered/breadcromb' %>

<div class="animated fadeIn">
  <div class="row">

    <div class="col-md-12">
      <div class="card">
        <div class="card-header">
          <strong class="card-title"><%= title('Notificações') %></strong>
        </div>
        <div class="card-body">
          <table id="bootstrap-data-table-export" class="table table-striped table-bordered">
            <thead>
              <tr>
                <th>Titulo:</th>
                <th>Para:</th>
                <th>Agendado:</th>
                <th>Anotações:</th>
                <th>Ação</th

                </tr>
              </thead>
              <tbody>
            <% @notification.each do |notification| %>
                <tr>
                  <td><%= notification.title %></td>
                  <td><%= notification.client.name %></td>
                  <td>  </td>
                  <td><span class="badge badge-info"> </span></td>
                  <td><%= link_to "Excluir", notification_path(notification), method: :delete, data: {confirm: "Deseja deletar esse item?"}, class: "btn btn-danger" %></td>
                  <td>
          <% end %>


                  </td>
                </tr>


              </tbody>
            </table>
            <p></p>


    <strong class="card-title">Notificações enviadas</strong>

<p></p>
<% @notification_send.each do |sender| %>
    <div class="list-group">
      <a href="#" class="list-group-item ">
        <span class="fa fa-paper-plane"></span>
      <span class="pull-right">

      </span><%= sender.title %>
        <span class="pull-right">
          <button class="btn btn-xs btn-info">Agendar</button>

          <%= link_to "<i class='fa fa-trash'></i>".html_safe, notification_path(sender), method: :delete, data: {confirm: "Deseja deletar esse item?"}, class: "btn btn-xs btn-danger" %>


        </span>
      </a>
    </div>
<% end %>

          </div>
        </div>

      </div>


    </div>
  </div><!-- .animated -->

  <!-- Card Aniversário  -->



</div><!-- .content -->
